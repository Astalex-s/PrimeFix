/**
 * Страница администратора (autonode.ru/admin).
 * В дальнейшем здесь будет авторизация через admin_settings.
 */
document.querySelector('#admin-login-form')?.addEventListener('submit', (e) => {
  e.preventDefault();
  // Авторизация будет реализована позже
});
